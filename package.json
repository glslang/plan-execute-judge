{
  "name": "plan-execute-judge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generic plan -> execute -> judge orchestration built on the Claude Agent SDK (TypeScript).",
  "license": "MIT",
  "engines": {
    "node": ">=22.9"
  },
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit",
    "test": "npm run build && node --test \"dist/*.test.js\"",
    "start": "node --env-file-if-exists=.env dist/index.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.204",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^7.0.2"
  }
}
