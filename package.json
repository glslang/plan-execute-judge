{
  "name": "plan-execute-judge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generic plan -> execute -> judge orchestration for Claude or Codex (TypeScript).",
  "license": "MIT",
  "engines": {
    "node": ">=22.9"
  },
  "scripts": {
    "build": "tsc -p .",
    "build:bun": "bun build ./src/index.ts --target=node --format=esm --packages=external --sourcemap=external --outdir=dist-bun",
    "typecheck": "tsc -p . --noEmit",
    "test": "npm run build && node --test \"dist/*.test.js\"",
    "start": "node --env-file-if-exists=.env dist/index.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.204",
    "@openai/codex-sdk": "^0.144.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
