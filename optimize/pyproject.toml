[project]
name = "pej-optimize"
version = "0.1.0"
description = "GEPA prompt-optimization harness for plan-execute-judge"
requires-python = ">=3.10"
dependencies = ["gepa==0.1.3"]

[tool.uv]
package = false
